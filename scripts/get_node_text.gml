//arg0 - node

node = argument0;

str = "ID : " + string(node.id) + "G : " + string(node.G) + " ; atkNode : " + string(node.attackNode) + " ; cost : " + string(node.cost) + " parent : " + string(node.parent);

return str;

